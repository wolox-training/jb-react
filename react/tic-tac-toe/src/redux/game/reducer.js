import actions from './actions'

const initialState = {

};

function reducer(state = initialState, action){
    switch(action){
        default:
            return state;
    }
}

export default reducer;