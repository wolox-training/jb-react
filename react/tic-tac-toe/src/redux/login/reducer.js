import actions from './actions';

const initialState = {
  isLoading: false,
  hasError: false,
  token: null
}

function reducer(state = initialState, action){
  switch(action.type){

  }
}

export default reducer;